# All paths

/*
    # Block site from being framed with X-Frame-Options and CSP
    Content-Security-Policy: frame-ancestors 'none'
    X-Frame-Options: DENY

    # Prevent browsers from incorrectly detecting non-scripts as scripts
    X-Content-Type-Options: nosniff

    # Force browsers to always go through HTTPS
    Strict-Transport-Security: max-age=31536000; includeSubDomains

    # Don't load any resource type not explicitly enabled
    # Disable plugins like Flash or Silverlight
    # Send reports to report-uri.io
    # Content-Security-Policy: default-src 'self'; object-src 'none'; img-src *; style-src *; font-src fonts.googleapis.com; media-src video.vibbio.com; script-src 'self' *.hs-analytics.net js.usemessages.com js.hsleadflows.net js.hs-scripts.com *.licdn.com connect.facebook.com *.googletagmanager.com code.jquery.com px.ads.linkedin.com;
